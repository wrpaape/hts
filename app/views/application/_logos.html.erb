<% attrs = [:slogan, :path_file, :path_default, :path_show, :class_name, :filename] %>
<% logos.each do |logo| %>
  <% logo = Hash[attrs.map { |attr| [attr, logo.send(attr)] }] %>
  <%= render partial: "logo", locals: logo %>
<% end %>